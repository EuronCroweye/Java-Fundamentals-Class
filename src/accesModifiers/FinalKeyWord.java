package accesModifiers;

public class FinalKeyWord {

	 public String GetInfo() {
			return ("final has numerous way to use:"
					+ "A finale class cannot be subclassed"
					+ "A finale method cannot be overridden by subclasses"
					+ "A finale variable can only be inherited once ");
	 }
}
